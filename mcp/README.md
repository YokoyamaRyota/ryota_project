# MCP Catalog

This directory defines project-local MCP metadata that may be useful for future workspace integrations.

## Notes

- The current GitHub Copilot installer does not materialize MCP runtime configuration from this catalog.
- Risk levels and profile groupings are still maintained as source metadata.
- Use `node scripts/build-mcp-config.js --validate` to validate catalog consistency.